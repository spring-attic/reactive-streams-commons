package rsc.documentation;

/**
 * Indicates the type of an operator.
 */
public enum OperatorType {
    TRANSFORMATION,
    FILTER,
    COMBINATION,
    CONDITIONAL,
    AGGREGATION,
    CONNECTABLE,
    ERROR_HANDLING,
    TIMED,
    PEEK,
    UTILITY
}
